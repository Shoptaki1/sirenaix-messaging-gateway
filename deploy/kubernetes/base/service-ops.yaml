apiVersion: v1
kind: Service
metadata:
  name: sirenaix-gateway-ops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sirenaix-gateway
  ports:
    - name: ops
      port: 9090
      targetPort: ops
