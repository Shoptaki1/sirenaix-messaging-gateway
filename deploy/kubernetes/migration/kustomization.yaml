apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Replace this with a new DNS-safe release identifier for every execution.
# A unique name avoids trying to mutate an immutable, previously applied Job.
nameSuffix: -REQUIRED_RELEASE_ID
resources:
  - job.yaml
